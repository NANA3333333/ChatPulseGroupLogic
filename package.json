{
  "name": "chatpulse-group-logic",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "WeChat-style SillyTavern group chat extension with per-member proactive messages and role-specific group API routing.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check automation-core.js && node --check index.js && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "CC-BY-NC-ND-4.0"
}
