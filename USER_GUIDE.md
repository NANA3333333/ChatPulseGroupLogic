# ChatPulse Group Logic 使用说明

适用版本：`0.4.0`

ChatPulse Group Logic 是一个运行在 SillyTavern 页面内的独立群聊扩展。它复用你已有的角色卡、User 人设、世界书和生成 API，但不会创建或修改 SillyTavern 原生群聊。

## 一、开始前的准备

第一次创建群聊前，请先确认：

1. SillyTavern 的普通角色私聊能够正常生成回复。
2. SillyTavern 中至少已有一张角色卡。
3. 如果要用特定身份进入群聊，请提前在 SillyTavern 中建立对应的 User 人设。
4. 扩展面板里的 `ChatPulse Group Logic` 已启用。

默认群聊直接复用 SillyTavern 当前连接的模型/API，因此第一次使用不必重复填写 Key。0.4.0 起可在成员设置里为某个角色选配专属群聊 Endpoint、Model 与 Key；没有配置或选择“跟随 SillyTavern 当前 API（默认）”的角色仍走 ST 当前 API，明确选择“此角色使用专用 API”但配置不完整时则直接报错。角色群聊 API、长期记忆的总结小模型、以及独立项目 [ST-AutoPulse](https://github.com/NANA3333333/ST-AutoPulse) 的私聊嫉妒 API 彼此独立。世界书不是开始使用的前置条件。

## 二、第一次进入：跟随新手引导

首次安装且还没有本地群聊时，扩展会自动显示新手任务。

- 屏幕会暂时变暗。
- 当前需要操作的按钮会被高亮。
- 引导卡与目标按钮之间会显示箭头。
- 带“请直接点击箭头指向的控件”的步骤，需要真实点击对应按钮。
- 输入群名、选择成员等内容会作为草稿保存。意外关闭后，再打开群聊中心可以继续。
- 按 `Esc` 或关闭群聊中心会暂停引导，不会删除已填内容。
- 刷新页面后也会恢复暂停的步骤；创建完成后的教学会自动回到刚创建的群。
- 点击“跳过引导”可以立即结束；之后仍可从帮助中心重新开始（电脑端 `?`；手机端 `··· → 使用帮助`）。

新手任务一共会带你完成这些操作：

1. 找到并点击 ChatPulse 群聊入口。
2. 点击 `＋` 发起群聊。
3. 输入一个群聊名称。
4. 确认你在本群使用的 User 人设。
5. 勾选至少一位角色。
6. 点击“创建群聊”。
7. 认识消息输入框与 `@` 规则。
8. 认识表情与红包；电脑端同时认识标题栏的按条删除入口。
9. 打开群管理。
10. 展开成员设置，认识本群主动发言与群嫉妒。
11. 认识角色专属群聊 API 与默认回退规则。
12. 了解长期记忆和权限设置。
13. 找到帮助中心（电脑端 `?`；手机端“聊天信息 → 使用帮助”）。

引导只会替你完成建群流程，不会自动发送消息，也不会擅自调用模型。

## 三、创建第一个群聊

### 1. 打开群聊中心

点击 SillyTavern 顶部的群聊图标。若顶部入口不可用，桌面端的备用入口可拖动；手机触摸布局的备用入口直接点按即可打开。

### 2. 打开创建窗口

可以从以下任一位置点击 `＋` 或“发起群聊”：

- 电脑端群聊列表标题旁；
- 手机端群聊列表标题栏；
- 没有群聊时的中央欢迎页。

### 3. 填写群名

群名用于区分不同剧情或角色组合。

- 引导过程中需要填写一个名字才能继续。
- 平时手动建群时可以留空，扩展会根据成员自动命名。
- 创建后可在“群管理 → 群名称”中修改。

### 4. 选择你的身份

“你在这个群中的身份”来自 SillyTavern 已有的 User 人设。

这个选择会影响：

- 你的消息名称与头像；
- 角色接收到的用户设定；
- 你发送或领取红包时的身份。

不同群可以绑定不同的 User 人设。没有可用人设时，扩展会使用 SillyTavern 当前用户名和默认头像显示，但不会启用跨私聊或跨群记忆。User 人设是跨聊记忆的身份边界：没有明确 persona 或 persona 不同都不会共享。

### 5. 选择群成员

勾选至少一张角色卡。可以使用搜索框按角色名筛选。

- 创建按钮旁会显示已选择人数。
- 至少选择一位成员后，“创建群聊”按钮才可点击。
- 创建后仍可在群管理中继续拉人或踢人。
- 成员可以来自不同的 SillyTavern 角色卡，不需要先把它们做成原生 Tavern 群。

### 6. 完成创建

点击“创建群聊”。创建成功后会自动进入新群，并把它设为当前群聊。之前创建的群仍保留在群列表；电脑端直接点击左侧群名切换，手机端先点聊天页左上角返回箭头，再选择其他群。

## 四、发送消息和 `@` 规则

在下方输入框输入消息，点击“发送”。

电脑端快捷键：

- `Ctrl + Enter`：发送；
- `⌘ + Enter`：macOS 发送；
- 单独按 `Enter`：换行。

回复顺序规则：

| 输入方式 | 群成员如何回复 |
|---|---|
| 普通消息，不写 `@` | 所有成员随机排序，依次自然接话 |
| `@角色名` | 被点名角色优先回复，其他成员随后继续 |
| `@全体` | 向全体成员发言，成员顺序仍会随机 |
| 角色在回复中 `@` 另一角色 | 本轮结束后，被点名角色会追加回应 |

输入 `@` 后会打开成员选择菜单。可继续输入角色名缩小范围，再点击对应角色。

## 五、聊天区按钮说明

| 按钮 | 功能 | 使用方法 |
|---|---|---|
| `＋` | 新建群聊 | 打开建群窗口 |
| `?` / `··· → 使用帮助` | 帮助中心 | 电脑端使用 `?`；手机端从“聊天信息”进入。可查看按钮图鉴、规则、权限和排错说明，也可重走引导 |
| `☺` | 表情 | 打开表情面板；选中的表情会插入输入框，不会立即发送 |
| 输入框旁圆形 `＋` | 更多 / 红包 | 打开更多功能；当前可直接进入红包窗口 |
| `🗑` / `··· → 选择删除` | 按条删除 | 电脑端点标题栏垃圾桶；手机端从“聊天信息”进入 |
| `···` | 群管理 | 打开成员、人设、记忆、世界书、队列和高级设置 |
| “打断” | 停止当前生成 | 请求停止弹窗内正在进行的回复队列 |

## 六、红包

点击输入框旁的圆形 `＋`，进入红包功能。

### 拼手气红包

填写红包总金额和份数，每份金额随机分配。

### 普通红包

填写单份金额和份数，每份金额相同。

红包发出后：

1. 红包卡片会写入当前群消息；
2. 群成员会开始一轮自然反应；
3. 领取结果会保存在当前群；
4. 可在“群管理 → 红包记录”中查看。

删除红包对应的消息时，关联的红包记录也会一起删除。

## 七、按条删除消息

1. 电脑端点击标题栏的 `🗑`；手机端点击 `··· → 选择删除`。
2. 直接点击聊天区中要删除的消息。
3. 顶部删除栏会显示已选择数量。
4. 点击“删除”并确认。
5. 点击“取消”可退出选择模式。

群管理中也提供消息列表式的选择删除。删除所有历史或整个群聊属于危险操作，需要再次确认。

## 八、群管理

点击右上角 `···` 打开群管理。顶部快捷标签可以直接跳到成员、记忆、世界书等区域；手机端顶部还提供“使用帮助”和“选择删除”快捷入口。

设置标题旁的范围标签非常重要：

- “当前群”：只影响现在打开的群；
- “所有群共用”：会影响全部 ChatPulse 群；
- “当前任务”：只描述正在执行的队列。

### 群名称

修改输入框内容并点击确认按钮。电脑端也可按 `Enter`。

### 你在群里的身份

切换本群绑定的 User 人设。不会影响其他群。

### 群成员

- “添加成员”：从未入群的角色中选择并加入；
- “移出”：把对应角色移出当前群。

成员变化会插入系统公告，并可能触发其余群成员的自然反应。

点击成员卡可展开该角色的群聊设置。

#### 本群主动发言

这些设置只影响“当前群里的这个角色”：

- 开启/关闭主动发言；
- 最短与最长间隔（每次会在区间内重新抽取）；
- 主动发言提示词；
- 下一次触发时间和最近状态；
- “立即测试一条”用于立即让该角色发送一条独立群消息。

同一角色加入两个群时，两边的开关和间隔完全独立。浏览器关闭期间不会积累一串待补发消息；再次打开后会从当前时间重新安排。主动消息始终只追加当前角色的一条消息，即使正文出现 `@另一角色` 或红包标签，也不会触发群轮询、成员接龙、红包反应或第二条嫉妒消息。

#### 群嫉妒

群嫉妒是本群主动发言的一部分，只有开启主动发言后才有机会生效：

- 独立开关与概率；
- 可自定义嫉妒提示词；
- 仅在该角色上次发言后又有其他成员发言时参与判断；
- 命中时会改变这一次主动消息的语气，不额外发送通知或第二条消息。

它不读取、修改或触发 ST-AutoPulse 的私聊嫉妒配置。

#### 角色群聊 API

这组设置按角色保存，会用于该角色参加的所有 ChatPulse 群：

- `跟随 SillyTavern 当前 API（默认）`：继续使用 SillyTavern 当前连接；
- `此角色使用专用 API`：填写 OpenAI-compatible Endpoint、Model 和 API Key；
- 可读取模型列表、测试连接、保存或清除 Key；
- Key 输入框留空保存时保留原 Key；只有“清除专用 Key”才真正删除，并同时改回跟随 SillyTavern 当前 API。

明文 Key 写入 SillyTavern Secrets；群数据和浏览器 localStorage 只保存非敏感配置与 Secret ID。若明确选择自定义模式但 Endpoint、Model 或 Key 不完整，扩展会直接提示错误，不会偷偷改用 ST 当前 API。长期记忆的“总结小模型”仍是独立设置。

### 本群 AI 行为

- “禁止 AI 因互相 @ 追加回复”：只阻止角色因另一位角色的 `@` 而在本轮后追加回复，不会阻止用户消息触发正常群轮询；
- “跨聊原文备用条数”：权限允许时，私聊或其他群最多注入多少条近期原文；
- “群聊上下文条数”：每次生成时保留多少条本群近期消息。

“群聊上下文条数”控制这次角色回复实际看到的本群近期消息；长期记忆里的 `R 原文窗口` 则决定哪些旧消息暂时保留原文、哪些可以进入摘要。它们是两个独立参数。

### 生成节奏

这是所有 ChatPulse 群共用的设置：

- 初始 API 间隔；
- 每次递增间隔；
- 最大退避间隔；
- 单个角色输出上限。

遇到 API 速率限制时，可适当增加间隔。

### 群聊世界书

- 可以额外勾选供本群使用的世界书；
- 可开启“同时读取成员角色卡世界书”；
- 世界书不是必填项。未选择时仍会使用角色卡、群历史和 User 人设。

### 长期记忆

长期记忆默认针对每个群独立工作：

- `R 原文窗口`：最近 R 条消息保留原文；
- `S 触发阈值`：窗口外未总结消息累计到 S 条时，在回复前先总结；
- “立即总结”：手动触发当前群总结；
- “清空摘要”：只清空摘要，不删除原始消息。

总结模型可选择：

- 当前模型；
- 自定义 OpenAI-compatible 小模型的 Endpoint 与 Model。

### 记忆权限

| 开关 | 默认值 | 作用 |
|---|---|---|
| 私聊可读取本群记忆 | 开 | 相关角色的普通私聊可以读取本群长期摘要与摘要之后的近期公开原文 |
| 本群可读取角色私聊记忆 | 关 | 允许本群生成时引用成员的私聊记忆 |
| 本群可读取角色其他群记忆 | 关 | 允许本群读取成员参与的其他群长期摘要与近期公开原文 |

建议只在剧情确实需要串联时开启后两项。开启“本群读取”权限时，如果“跨聊原文备用条数”还是 0，扩展会自动设为 12 条，避免出现“开关开了但完全没有注入”的情况。

三条共享路径都会先检查 User 人设：

- 私聊 → 群聊：私聊保存时使用的 User 必须与本群一致；
- 群聊 → 私聊：当前私聊的 User 必须与来源群一致；
- 其他群 → 本群：两个群绑定的 User 必须一致。

满足同一 User 后，仍要有对应权限才会共享。群聊注入私聊时会同时提供长期摘要和摘要之后的最新公开原文，避免只记得旧进度。

出于隔离安全，缺少 SillyTavern persona 元数据的旧私聊文件不会作为跨聊记忆来源；在对应 User 下继续一次私聊并正常保存后，新记录即可参与授权共享。

### 独立预设 / 正则

可填写或导入仅供 ChatPulse 群聊使用的附加预设和文本替换正则。该区域为所有 ChatPulse 群共用，导入前请确认内容可信。

### 运行队列

显示当前轮询中的：

- 正在回复的角色；
- 等待中的角色；
- 当前等待或退避状态。

“跳过”会丢弃当前角色返回的结果；“停止”会终止后续队列。已经发出的网络请求可能仍会返回，但结果会被忽略。

为避免回复写入错误的群，生成队列结束前不能切换或删除当前群。请先点“停止”，再等待当前请求返回。

### 最近输入 / 输出

用于排查模型不回复、格式异常等问题，包括：

- 实际发送给模型的 Prompt；
- Raw Output；
- 清理后的输出；
- 是否发生重试。

这些调试记录按当前群显示，但只保存在当前页面会话中。失败请求也会记录脱敏后的错误；刷新页面会自动清除，不会把完整 Prompt 写入 localStorage。

### 危险操作

- 清空队列；
- 删除全部对话与红包记录；
- 删除当前群聊。

删除当前群聊会同时删除它的消息、红包和长期摘要，并清除当前页面会话里的该群调试记录，无法从扩展界面撤销。

## 九、手机端使用

- 群聊中心会使用全屏布局，进入时先显示微信式群聊列表。
- 点击群聊条目进入对话；聊天标题栏左上角的返回箭头可回到群聊列表。
- 群聊列表标题栏中的 `＋` 可直接新建群聊。
- 群管理会整页覆盖打开，点击左上角返回按钮或按系统键盘的 `Esc` 可返回。
- 使用帮助：点击聊天页右上角 `··· → 使用帮助`。
- 按条删除：点击聊天页右上角 `··· → 选择删除`。
- 新手引导卡会自动放在目标控件的另一侧，避免挡住需要点击的位置。
- 软键盘弹出时，界面会根据可视区域重新定位。

## 十、数据保存与隐私

以下内容保存在当前浏览器的 `localStorage`：

- 群聊列表；
- 群成员和本群 User 人设；
- 消息与红包；
- 长期摘要；
- 新手引导进度与建群草稿。
- 每个群成员在该群里的主动发言、群嫉妒、下一次触发与 claim 状态。

最近 Prompt、Raw Output、清理后输出和失败诊断只保存在页面内存，刷新即清除。

角色专属群聊 API 的 Endpoint、Model、模式和 Secret ID 保存在 SillyTavern 扩展设置；明文 Key 只保存在 SillyTavern Secrets，不写入 localStorage、测试报告或截图。

注意：

- 它们不会创建 SillyTavern 原生群聊；
- 清除该站点的浏览器数据可能会同时清除这些内容；
- 换浏览器、换设备或换访问地址时，数据不会自动迁移；
- 扩展本身不提供云端同步。
- 同一浏览器、同一 SillyTavern 地址下同时打开的标签页，会在 localStorage 变化时刷新群列表与本地状态；正在生成的页面仍应等队列结束后再切群。

## 十一、常见问题

### 点击发送后没有回复

依次检查：

1. SillyTavern 普通私聊是否可以生成；
2. 当前群是否还有有效角色成员；
3. 运行队列是否正在等待 API 间隔或速率退避；
4. 长期记忆总结是否失败并中止了本轮；
5. “最近输入 / 输出”里是否有失败诊断、Prompt 或 Raw Output；
6. 浏览器控制台是否记录加载错误。

先确认该角色的“角色群聊 API”模式：选“跟随 SillyTavern 当前 API（默认）”时，请在 SillyTavern 原生连接设置中处理 Key、模型、余额或网络问题；选“此角色使用专用 API”时，请检查成员卡内的 Endpoint、Model、Key 状态与连接测试。长期记忆总结失败则另查“总结小模型”，不要把两套路由混在一起。

### 群主动消息为什么没有触发

依次检查：

1. 当前群的该成员是否开启主动发言；
2. 最短/最长间隔是否合理，下一次触发是否仍在未来；
3. 普通群轮询或另一个主动请求是否正在生成；
4. 该角色的群聊 API 是否完整可用；
5. 浏览器页面是否仍在运行。群主动由前端调度，关闭全部 SillyTavern 页面后不会继续计时生成。

如果需要关闭浏览器后仍可排队的**私聊**主动消息，请使用独立项目 [ST-AutoPulse](https://github.com/NANA3333333/ST-AutoPulse)；它不负责本群主动发言。

### 创建按钮不能点击

必须至少勾选一位角色。若角色列表为空，请先在 SillyTavern 导入或创建角色卡，再重新打开建群窗口。

### 找不到想用的 User 人设

先在 SillyTavern 中创建或启用该 User 人设，再刷新页面。群聊选择框只会列出 SillyTavern 当前可用的人设。

### 世界书必须挂吗

不必须。没有群级世界书、角色卡也没有绑定世界书时，扩展仍会使用角色卡设定、User 人设、本群近期消息和长期摘要生成。世界书只用于补充额外背景。

### 创建第二个群后，第一个群“不见了”

新群只会成为当前打开的群，不会覆盖旧群。电脑端查看左侧群列表；手机端在聊天页点击左上角返回箭头，再选择原来的群。

### 可以把不同角色卡放进同一个群吗

可以。建群时直接勾选多张现有 SillyTavern 角色卡，或创建后从“群管理 → 添加成员”继续加入。无需先建立 SillyTavern 原生群聊。

### 私聊和群聊会不会串 User

默认不会。跨私聊或跨群记忆必须同时满足“相同 User 人设”和“对应权限已开启”。切换成另一个 User 后，新身份不会读到前一个身份授权的记忆。

### 手机端找不到群聊列表

在聊天页点击左上角返回箭头。群聊列表会全屏显示；选择群聊后列表会自动收起。

### 不小心关闭了新手引导

重新打开群聊中心会尝试继续已暂停的步骤。也可以在电脑端点击右上角 `?`，或在手机端点击 `··· → 使用帮助`，再点“重新开始新手引导”。

### 想完全重新看一遍

打开帮助中心（电脑端 `?`；手机端 `··· → 使用帮助`），点击底部“重新开始新手引导”。这不会删除已有群聊。

## 十二、推荐的第一次体验顺序

1. 用两位角色创建一个测试群。
2. 发送一条不带 `@` 的普通消息，观察随机轮询。
3. 再发送一条 `@某角色` 的消息，对比回复顺序。
4. 发送一个小额测试红包。
5. 打开群管理，确认本群 User 人设和成员。
6. 查看长期记忆默认值与三个权限开关。
7. 再创建一个使用不同成员组合的群；电脑端通过左侧群列表切换，手机端点左上角返回后在全屏群列表切换。
8. 打开“最近输入 / 输出”，了解出现问题时去哪里排查。
9. 展开两位成员的设置，给其中一位开启较短的测试间隔并点“立即测试一条”；确认只新增这一位角色的一条消息。
10. 为第二位角色设置不同间隔，验证两个倒计时互不覆盖；需要时再测试群嫉妒和角色专属 API。
